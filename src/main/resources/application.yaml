spring:
  application:
    name: store
  datasource:
    url: jdbc:mysql://localhost:3306/store?createDatabaseIfNotExist=true
    username: root
    password: mysql
stripe:
  apiUrl: https://api.stripe.com
  enabled: true
  timeout: 3000
  supported-currencies: USD,EUR,TL

payment-gateway: paypal